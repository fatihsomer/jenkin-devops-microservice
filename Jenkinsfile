pipeline {
	agent any
	stages {
		stages('Build') {
			steps {
				echo "Build"
			}
		}
		stages('Test') {
			steps {
				echo "Test"
			}
		}
		stages('Integration Test') {
			steps {
				echo "Integration Test"
			}
		}
	}
}
